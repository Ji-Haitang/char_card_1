- 角色卡预设使用 `identifier` 唯一标识每个 prompt 片段，并通过 `prompt_order[].order` 数组显式声明注入顺序，而非依赖 JSON 键序。
- 可插拔的提示块统一包含 `system_prompt`、`role`、`enabled`、`marker`、`injection_position`、`injection_depth`、`forbid_overrides` 等元数据字段，便于运行时动态开关与排序。
- 文风规范采用 `<tag>` 包裹 YAML 的结构化文档形式，通过 `directives[].tactics` 嵌套 id→title→method→example 层级组织写作规则。
- 版本化文件名遵循 `名称-v主.次.修订.apk/png/json` 的语义化命名约定，便于按版本号检索历史产物。
EJS 模板语法（`<%_ ... _%>`）+ 自定义 XML 标签契约（`<SLG_MODE>/<MAIN_TEXT>/<SUMMARY>/<SIDE_NOTE>`）；字段占位符采用 `{{type: 约束}}` 形式（如 `{{enum: <NpcList>中的NPC名}}`、`{{percent: 10%-90%}}`、`{{HH:MM: ...}}`），由上层引擎解析为结构化 JSON。
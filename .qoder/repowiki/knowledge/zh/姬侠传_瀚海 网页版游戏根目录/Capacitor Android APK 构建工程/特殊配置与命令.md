先执行 `npm run build`（即 `node build.js`）将游戏资源镜像到 `www/`，再运行 `npm run sync`（内部调用 `npx cap sync android`）同步至 Android 工程，最后用 `./gradlew assembleRelease` 或 Android Studio 构建 APK；图标更新需单独执行 `node gen-icons.js`。
# 姬侠传 历史管理方案

## 一、现状与问题

### 1.1 当前机制（Phase 1/2 的滑动窗口）

每轮 AI 回复解析出 `<SUMMARY>` 后写入 `summaryHistory[]`，构建 prompt 时注入为 `<RecentHistory>` 块。当记录超出 token 预算时，直接丢弃最旧的条目。

**核心缺陷**：被截断的旧事件**永久丢失**，模型无法再感知。对于有多条隐藏剧情线的游戏（如 NPC 好感度发展、未解决的悬念），截断会导致剧情连贯性下降。

### 1.2 对比开源方案

| 方案 | 优点 | 缺点 | 适合本项目？ |
|------|------|------|-------------|
| MemGPT / Letta | 自动记忆管理，理论完备 | 需要服务端进程，不兼容静态部署 | ❌ |
| Mem0 | 结构化记忆提取 | 同上，需后端服务 | ❌ |
| LangChain / LlamaIndex | 完整 RAG 框架 | 后端 Python，无法运行在浏览器/APK | ❌ |
| Transformers.js（本地 embedding） | 无服务端依赖 | 模型文件 20-50MB，首次下载慢，移动端不友好 | ⚠️ 备选 |
| **硅基流动 embedding API** | 纯 API 调用，零本地文件，中文效果好 | 需用户配置 Key | ✅ **采用** |

**结论**：采用硅基流动的 `BAAI/bge-m3` embedding API 做语义向量化召回，未配置或 API 失败时降级为现有截断方案。

---

## 二、整体架构

```
用户执行行动
  ↓
pipeline.runTurn()
  ├── [请求阶段]  构建 prompt → API 请求（无副作用）
  └── [提交阶段]  解析 → 更新状态 → 渲染 → 存档
                    ↓（异步，不阻塞）
             新 SUMMARY 调 embedding API
             → 向量存入 IndexedDB embeddings store

下一轮构建 prompt 时：
  1. 计算 query embedding（用户输入 + 最近 2 条 SUMMARY）
  2. 内存中余弦相似度，Top-K 召回（排除已在滑动窗口内的条目）
  3. 拼接为 <RecentHistory> 块注入 prompt
     - 向量召回部分：[相关历史回忆]
     - 滑动窗口部分：[近期经历]
  4. embedding 不可用时：完全跳过召回，走现有截断逻辑
```

---

## 三、不影响 SR/ST 链路的保证

- 所有 embedding 逻辑仅在 `!getRenderFunction()`（独立模式）下执行
- prompt-builder 的召回注入分支仅在 `embeddingEnabled && isStandalone` 时激活
- SR/ST 模式不加载 embedding 相关模块，不产生任何额外请求

---

## 四、分阶段实施计划

### Phase 3.1 — Embedding 服务层 + 向量存储

**目标**：建立 embedding 能力的基础设施，能对文本计算向量并持久化存储。

#### 4.1.1 新建 `module/embedding-service.js`

| 功能 | 说明 |
|------|------|
| 调用硅基流动 `/v1/embeddings` | 格式兼容 OpenAI Embeddings API |
| 批量 embed | 多条文本一次请求（节省 API 调用次数） |
| 多 Key 轮询 | 支持逗号分隔多个 Key，每次请求自动轮询到下一个（均摊免费 Key 的 QPS 限额） |
| AbortController 超时 | 3 秒超时，使用 `AbortController` + `setTimeout` 实现精确中断，失败时返回 null |
| Engine Fingerprint | 记录当前 embedding 模型标识（如 `bge-m3@1024d`）；用户切换模型后，标记旧向量需重建 |
| 配置项 | endpoint（默认 `https://api.siliconflow.cn/v1`）/ apiKey（支持多 Key）/ model（默认 `BAAI/bge-m3`）/ enabled 开关 |

**请求格式**：
```json
POST /v1/embeddings
{
  "model": "BAAI/bge-m3",
  "input": ["文本1", "文本2"],
  "encoding_format": "float"
}
→ { "data": [{ "embedding": [0.1, 0.2, ...], "index": 0 }, ...] }
```

#### 4.1.2 改造 `module/storage-service.js` — IndexedDB 新增 embeddings store

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 对应 summaryHistory 条目的 id |
| vector | ArrayBuffer | bge-m3 输出 1024 维向量，以 Float32Array 序列化为 ArrayBuffer 存储（比 number[] 省 ~60% 空间） |
| text | string | 原始 summaryText（供调试展示） |
| week | number | 对应的游戏周数 |
| createdAt | number | 时间戳 |
| fingerprint | string | 生成该向量时的 Engine Fingerprint（模型切换后可判断是否需要重建） |

**向量序列化**（借鉴 LittleWhiteBox）：
```javascript
// 存储时：Float32Array → ArrayBuffer（紧凑二进制）
function float32ToBuffer(arr) {
    return arr.buffer.slice(arr.byteOffset, arr.byteOffset + arr.byteLength);
}
// 读取时：ArrayBuffer → Float32Array
function bufferToFloat32(buffer) {
    return new Float32Array(buffer);
}
```

**新增接口**：
- `saveEmbedding(id, vector, metadata)`
- `loadAllEmbeddings()` — 返回全部记录，启动时预热到内存缓存
- `deleteEmbedding(id)`
- `clearEmbeddings()` — 新游戏时调用

**内存缓存策略**：启动时一次性 load 全部向量到内存（数百条 × 1024 维 float32 ≈ 数 MB，可接受），查询时直接遍历，不重复读 IDB。

#### 4.1.3 新建 `module/memory-recall.js` — 召回引擎

**主接口**：
```javascript
recallRelevantMemories(queryText, topK, excludeIds)
// → [{ id, summaryText, week, similarity }, ...]
```

**实现逻辑**：
1. 调 embeddingService.embed(queryText) 获取 query 向量
2. 遍历内存缓存，计算余弦相似度
3. 过滤相似度 < 0.5 的条目
4. 返回 Top-K 条（按相似度降序）

**Token 估算**（借鉴 LittleWhiteBox，用于判断单条 SUMMARY 是否超出 embedding 输入限制）：
```javascript
function estimateTokens(text) {
    if (!text) return 0;
    const chinese = (text.match(/[\u4e00-\u9fff]/g) || []).length;
    const other = text.length - chinese;
    return Math.ceil(chinese + other / 4);
}
```

**余弦相似度**（纯 JS，无依赖）：
```javascript
function cosineSimilarity(a, b) {
    let dot = 0, normA = 0, normB = 0;
    for (let i = 0; i < a.length; i++) {
        dot += a[i] * b[i];
        normA += a[i] * a[i];
        normB += b[i] * b[i];
    }
    return dot / (Math.sqrt(normA) * Math.sqrt(normB));
}
```

---

### Phase 3.2 — Pipeline 集成 + Prompt 注入

**目标**：在现有流程中接入 embedding 存储和召回，不影响原有链路。

#### 4.2.1 改造 `module/pipeline.js` — 提交阶段末尾异步存 embedding

```
_commitResponse 完成后：
  if (embeddingEnabled && isStandalone) {
    // 异步，不 await，不阻塞后续流程
    embeddingService.embed(newSummaryText)
      .then(vector => storageService.saveEmbedding(summaryId, vector))
      .catch(e => console.warn('[Embedding] 存储失败，跳过:', e));
  }
```

#### 4.2.2 改造 `module/prompt-builder.js` — buildMessages 增加召回注入

**query 构成**：当前用户输入 + 最近 2 条 SUMMARY 拼接

**注入格式**（`<RecentHistory>` 内部分区）：
```
<RecentHistory>
[相关历史回忆]
- 第X年第Y月第Z周，主角在后山遇到钱塘君，两人交谈了武学心得...
- ...

[近期经历]
- 第A年第B月第C周，主角...
- ...
</RecentHistory>
```

**Token 预算分配**：
- 召回部分上限：3000 token 或 15 条记录（取先触及者，可配置）
- 滑动窗口部分：占剩余历史预算

**降级路径**：embedding 不可用 → 不注入召回部分 → `<RecentHistory>` 结构不变

---

### Phase 3.3 — 配置 UI + 调试面板

**目标**：让用户能方便地配置和验证 embedding 功能。

#### 4.3.1 改造 `ui/config-modal.js` — 新增 Embedding 配置区域

**UI 布局**（在主 API 配置下方）：
```
── Embedding 配置（可选）──────────────────
  启用向量记忆：[☑ 开关]
  Embedding API 地址：[https://api.siliconflow.cn/v1]
  Embedding API Key ：[必填，不复用主 API Key]
  模型：[BAAI/bge-m3 ▼]
  [🔗 测试 Embedding]
  状态：🟢 可用，1024 维 / ⚪ 未配置 / 🔴 不可用
───────────────────────────────────────────
```

**Key 规则**：Embedding API Key 独立配置，不与主 API Key 共享。Key 为空时视为 embedding 功能未启用，直接走截断降级。

#### 4.3.2 调试面板增强 — 新增"记忆召回"标签页

| 展示内容 | 说明 |
|----------|------|
| 本轮召回的 memories | 文本摘要 + 相似度分数 |
| 索引状态 | 已索引 N 条 / 总 M 条 |
| 重建记忆索引按钮 | 批量补算所有已有 SUMMARY |

---

### Phase 3.4 — 存档兼容与边界处理

**目标**：确保存档导入/导出不丢失向量数据，并兼容没有 embedding 数据的老存档。

#### 4.4.1 存档导出/导入适配

- **导出**：存档 JSON 新增 `embeddings` 字段（向量序列化为 number[]）
- **导入**：恢复 embeddings 到 IDB + 内存缓存
- **老存档无 embeddings**：正常加载，新产生的 SUMMARY 逐步积累

#### 4.4.2 "重建记忆索引"功能

适用场景：
- 导入老存档后想启用 embedding 召回
- 首次启用 embedding 功能，对已有历史补算

实现方式：
- 遍历全部 summaryHistory，分批调 embedding API（每批 10 条）
- 进度条显示（已完成 X/Y）
- 支持中断（关闭弹窗则停止，已完成的保留）

---

## 五、降级策略

| 条件 | 行为 | 用户感知 |
|------|------|---------|
| 用户未配置 embedding | 完全跳过 | 与现有体验一致，无变化 |
| embedding API 调用失败（单次） | 弹窗提示用户；跳过本次存储/召回，走截断；未向量化的 SUMMARY 可事后通过"重建记忆索引"补算 | 弹窗提示一次，不重复打扰 |
| embedding API 超时（>3s） | 同上 | 弹窗提示一次，不重复打扰 |

**召回失败节流**（借鉴 LittleWhiteBox）：使用冷却期机制（如 10 秒），同一错误在冷却期内不重复弹窗：
```javascript
let lastFailAt = 0;
const COOLDOWN_MS = 10_000;
function canNotifyFail() {
    const now = Date.now();
    if (now - lastFailAt < COOLDOWN_MS) return false;
    lastFailAt = now;
    return true;
}
```
| IndexedDB 不可用 | 跳过向量持久化，当次内存可用 | 刷新后无历史记忆 |

---

## 六、涉及文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `module/embedding-service.js` | 新增 | 硅基流动 embedding API 封装 |
| `module/memory-recall.js` | 新增 | 余弦相似度 Top-K 召回引擎 |
| `module/storage-service.js` | 改造 | IndexedDB 新增 embeddings store + 内存缓存 |
| `module/prompt-builder.js` | 改造 | `buildMessages` 的 `<RecentHistory>` 注入逻辑 |
| `module/pipeline.js` | 改造 | 提交阶段末尾异步存 embedding |
| `ui/config-modal.js` | 改造 | 新增 embedding 配置区域 |
| `index.html` | 改造 | 引入新 script 标签 |

---



## 八、验收清单

| # | 场景 | 预期行为 |
|---|------|---------|
| 1 | 配置硅基流动 API → 点测试 | 显示 `🟢 可用，1024 维` |
| 2 | 正常游戏 10 轮 | DevTools IDB `embeddings` store 有 10 条记录 |
| 3 | 游戏 20+ 轮，行动涉及早期事件 | 调试面板显示召回了相关旧 SUMMARY（相似度 > 0.5） |
| 4 | 关闭 embedding 开关 | 无额外网络请求，游戏体验不变 |
| 5 | 模拟 embedding API 不可达 | 游戏不卡死，控制台 warn，正常走截断 |
| 6 | 导出存档 → 导入 | embeddings 随存档迁移，导入后立即可召回 |
| 7 | 点"重建记忆索引" | 进度条走完，所有历史 SUMMARY 均已索引 |
| 8 | SR/ST 模式运行 | 无任何 embedding 日志或请求 |

---

## 九、Phase 3 范围边界（不做的事）

| 排除项 | 说明 | 后续计划 |
|--------|------|---------|
| `bge-reranker-v2-m3` 重排序 | Phase 3 只用 bge-m3 做基础向量召回；reranker 是对召回结果做二次精排的额外步骤，复杂度高 | 若 Phase 3 召回效果不满意，再追加为 Phase 4 |
| 结构化事实提取 | 不对 SUMMARY 做 NLP 解析（人物关系、事件类型标注等）；保持每轮一条 SUMMARY 的原始文本粒度 | 暂无计划 |

---

## 十、后续演进路线（Phase 3.5 — Phase 5）

Phase 3 面向 ~1000 条 SUMMARY 足够好用；当重度用户积累到几千条对话时，可按需逐步升级：

### Phase 3.5 — 轻量优化（纯前端，零额外 API 调用）

**触发条件**：Phase 3 上线后发现"召回噪声增多"或"旧事件偶尔漏召"。

| 优化项 | 做法 | 效果 |
|--------|------|------|
| 时间衰减加权 | `最终分 = 相似度 × (1 - α × 归一化时间距离)`，α 可配（建议 0.05-0.1） | 越久远的记忆需要更高相似度才能入围，减少"语义近但不相关"的旧条目 |
| 相似度阈值上调 | 从 0.5 → 0.6 | 直接砍掉低质量召回 |
| NPC 名字精确匹配加分 | 维护 `Map<NPC名, summaryId[]>`，query 中出现 NPC 名时，对应 SUMMARY 加 0.1 分 | 解决"用户提到具体人名但向量没排最前"的问题 |
| Top-K 扩大 + 去重合并 | 先取 Top-30，合并连续周数的相关记录（同事件线），再截取 15 条 | 减少重复、保留事件完整性 |

### Phase 4 — 关键词倒排 + Reranker（需额外 API 调用）

**触发条件**：Phase 3.5 后仍有"语义相似但不相关"的召回污染。

| 模块 | 做法 | 成本 |
|------|------|------|
| 轻量关键词索引 | 对每条 SUMMARY 提取 NPC 名 + 地点 + 关键物件，建立 `Map<关键词, summaryId[]>` | 零 API，纯前端 |
| 两路融合（W-RRF） | 向量召回 Top-30 + 关键词命中集 → 加权倒数排序融合 → 取 Top-15 | 零额外 API |
| Reranker 精排 | 融合后 Top-15 调 `bge-reranker-v2-m3` 做二次精排 | 每轮多一次 API（rerank 请求） |

**为什么需要关键词补充向量**：向量擅长"大方向对"（语义相关），但遇到精确实体（如"钱塘君给我的那把剑"）时，关键词直接匹配"钱塘君"+"剑"比向量更准。两路融合取长补短。

### Phase 5 — LLM 关系图 + PPR 扩散（重度方案）

**触发条件**：需要"跨角色联想召回"能力（如跟 A 对话时自动召回 A 和 B 的过往恩怨）。

| 模块 | 做法 | 成本 |
|------|------|------|
| 三元组提取 | 每轮 AI 回复后，额外调一次 LLM 从 SUMMARY 提取 `{s, t, r}` 关系边 | 每轮多一次 LLM 请求（可用小模型降本） |
| 关系图存储 | IDB 新增 `edges` store，节点=角色/地点，边=互动关系+权重 | 零额外 API |
| PPR 扩散 | 从当前对话涉及的节点出发，Personalized PageRank 迭代 → 关联节点的记忆提权 | 纯 JS 计算（~50 行），零 API |

**三元组提取 prompt 参考**（借鉴 LittleWhiteBox）：
```
从一轮对话中提取关系三元组：
- s: 施事方（角色名）
- t: 受事方（角色名）  
- r: 互动行为（6-12字动作短语，如"以圣剑刺穿心脏"）
每轮 1-3 条，无明确互动时返回空数组。
```

**PPR 效果举例**：你在跟钱塘君说话 → 图中钱塘君与洞庭君关系边权高 → 扩散后洞庭君相关记忆被提权 → 即使用户没提到洞庭君，相关恩怨也会被召回注入 prompt。

---

### 演进路线总览

```
Phase 3（当前）── 纯向量 dense 召回 + 相似度排序 ──── 适用 ~1000 条
    ↓
Phase 3.5 ───── 时间衰减 + NPC精确匹配加分 ────── 适用 ~2000 条
    ↓
Phase 4 ─────── 关键词倒排融合 + Reranker ─────── 适用 ~5000 条
    ↓
Phase 5 ─────── LLM三元组 + 关系图 + PPR扩散 ──── 无上限
```

每个 Phase 独立可选，按实际召回效果决定是否升级。
